import Foundation

enum HTTPHeader {
    static let authorization = "Authorization"
    static let accept = "accept"
    static let contentType = "Content-Type"
}
enum ContentType {
    static let applicationJson = "application/json"
    static let multiPartFormData = "multipart/formdata"
}
